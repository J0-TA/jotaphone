import { createTheme } from "@mui/material/styles";


const theme = createTheme({
    palette: {
      mode: "light",
      primary: {
        main: "#0066cc",
      },
      background: {
        default: "#f4f6f8",
        paper: "#ffffff",
      },
    },
  });

  export default theme;